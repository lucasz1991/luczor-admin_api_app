; Keep PHP's inbound request boundary aligned with Nginx and Laravel.
; Laravel applies the precise byte-level proxy contract and incremental
; upstream response limits; PHP still fails closed before application code.
post_max_size = 16M
upload_max_filesize = 16M
max_input_time = 30
max_execution_time = 120
